<?php
        include "../../component/header.php";
        include "../pub/conn.php";



        // 解码jwt
        $session_id = $_GET['session_id'];
        $show_id = $_GET['show_id'];
        if($session_id)
        {
          // 获取show_id
        }
        else if($show_id)
        {
          // 获取session_id
        }


       


?>